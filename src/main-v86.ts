import * as xtermModule from '@xterm/xterm';
import * as fitModule from '@xterm/addon-fit';
import '@xterm/xterm/css/xterm.css';
import { attachGuestTelemetry, type GuestIdentity, type V86TelemetryTarget } from './v86-telemetry.ts';
import { VM_RESOURCE_POLICIES, VMRuntimeResourceEnforcer, type VMResourcePolicyName } from './engine/VMResourcePolicy.ts';
import { artifactForIsoUrl, fetchVerifiedIso } from './core/verified-iso-fetch.ts';

const TerminalCtor = xtermModule.Terminal;
const FitAddonCtor = fitModule.FitAddon;
declare global { interface Window { linuxLabVM?: V86LinuxTerminal; V86Starter?: new (o: Record<string, unknown>) => V86Runtime; V86?: new (o: Record<string, unknown>) => V86Runtime; } }
type V86Runtime = V86TelemetryTarget & { serial0_send: (d: string) => void; keyboard_send_text?: (d: string) => void; run?: () => void | Promise<void>; stop?: () => void; destroy?: () => void; add_listener: (e: string, cb: (v?: number) => void) => void; wait_until_vga_screen_contains?: (t: string | RegExp, o?: { timeout_msec?: number }) => Promise<void>; };
type Profile = { name: string; memoryMiB: number; cdrom: string; policy: VMResourcePolicyName; expectedGuest: 'alpine' };
const PROFILES: Record<'developer' | 'virt', Profile> = { developer: { name: 'Developer Alpine', memoryMiB: 1024, cdrom: '/api/iso', policy: 'developer', expectedGuest: 'alpine' }, virt: { name: 'Alpine Virt 3.24.1', memoryMiB: 512, cdrom: '/api/iso?image=virt', policy: 'virt', expectedGuest: 'alpine' } };
function initialProfile(): Profile { return new URLSearchParams(window.location.search).get('profile') === 'developer' ? PROFILES.developer : PROFILES.virt; }

export class V86LinuxTerminal {
  private term: xtermModule.Terminal; private fitAddon: fitModule.FitAddon; private emulator: V86Runtime | null = null; private telemetryDispose: (() => void) | null = null; private terminalDataDisposable: { dispose(): void } | null = null; private static runtimePromise: Promise<void> | null = null; private profile = initialProfile(); private enforcer: VMRuntimeResourceEnforcer | null = null; private bootController: AbortController | null = null; private bootId = 0; private bootTimeout: number | null = null; private sessionTimer: number | null = null; private ready = false; private vgaReady = false; private guestIdentity: GuestIdentity | null = null; private initializationFailure: Error | null = null; private health: 'booting' | 'ready' | 'offline' = 'booting'; private bootStage = 'starting'; private serial = ''; private lastSerialAt = 0;
  constructor(containerId = 'v86-terminal-container') { if (!TerminalCtor || !FitAddonCtor) throw new Error('Terminal runtime failed to load'); const container = document.getElementById(containerId); if (!container) throw new Error('Terminal container is missing'); this.term = new TerminalCtor({ cursorBlink: true, fontSize: 14, convertEol: true, scrollback: 10000 }); this.fitAddon = new FitAddonCtor(); this.term.loadAddon(this.fitAddon); this.term.open(container); this.terminalDataDisposable = this.term.onData(d => { if (this.canUseRuntime()) this.emulator?.serial0_send(d); }); window.addEventListener('resize', () => this.fit()); setTimeout(() => this.fit(), 100); this.bindControls(); void this.start(this.profile); }
  private bindControls(): void { document.getElementById('btn-v86-alpine')?.addEventListener('click', () => void this.start(PROFILES.developer)); document.getElementById('btn-v86-virt')?.addEventListener('click', () => void this.start(PROFILES.virt)); document.getElementById('btn-v86-restart')?.addEventListener('click', () => void this.start(this.profile)); document.getElementById('btn-v86-terminal')?.addEventListener('click', () => this.showTerminal()); document.getElementById('btn-v86-screen')?.addEventListener('click', () => this.showScreen()); document.getElementById('btn-v86-diagnostics')?.addEventListener('click', () => this.showDiagnostics()); document.getElementById('btn-v86-copydiag')?.addEventListener('click', () => void this.copyDiagnostics()); }
  private async start(profile: Profile): Promise<void> { const id = ++this.bootId; this.bootController?.abort(); this.bootController = new AbortController(); const signal = this.bootController.signal; await this.dispose(); if (id !== this.bootId || signal.aborted) return; this.profile = profile; this.enforcer = new VMRuntimeResourceEnforcer(VM_RESOURCE_POLICIES[profile.policy]); this.ready = false; this.vgaReady = false; this.guestIdentity = null; this.initializationFailure = null; this.serial = ''; this.lastSerialAt = 0; this.bootStage = 'starting'; this.setHealth('booting'); this.showTerminal(); this.term.clear(); this.status(`${profile.name} • checking runtime`); this.term.writeln(`LinuxTerminal — ${profile.name}`);
    try { await this.loadRuntime(); if (id !== this.bootId || signal.aborted) return; await this.preflightRuntimeAssets(signal); if (id !== this.bootId || signal.aborted) return; const artifact = artifactForIsoUrl(profile.cdrom); this.status(`${profile.name} • verifying image`); const isoBytes = await fetchVerifiedIso(profile.cdrom, signal); if (!isoBytes.byteLength) throw new Error('Verified Linux image is empty'); const screen = document.getElementById('screen_container'); if (!screen) throw new Error('VM screen container is missing'); const policy = this.enforcer; if (!policy) throw new Error('VM resource policy was not initialized'); const Runtime = window.V86Starter ?? window.V86; if (typeof Runtime !== 'function') throw new Error('Local v86 runtime did not expose a constructor'); this.bootStage = 'creating VM'; this.status(`${profile.name} • booting`); this.monitor(`${profile.memoryMiB} MiB • verified ${artifact.filename}`); const vm = new Runtime({ wasm_path: '/v86.wasm', memory_size: policy.memoryBytes, vga_memory_size: policy.vgaMemoryBytes, screen_container: screen, bios: { url: '/seabios.bin' }, vga_bios: { url: '/vgabios.bin' }, cdrom: { buffer: isoBytes }, boot_order: 0x213, fastboot: true, bootmenu: false, autostart: false, disable_speaker: true, net_device: { type: 'none' } }); if (!vm) throw new Error('Local v86 runtime failed to create an emulator'); this.emulator = vm; this.sessionTimer = window.setTimeout(() => this.fail('VM session resource limit reached'), policy.remainingSessionMs()); this.telemetryDispose = attachGuestTelemetry(vm, { onTelemetry: s => { if (id === this.bootId) this.monitor(`REAL guest • ${s.kernel} ${s.architecture} • ${s.cpuPercent.toFixed(1)}% CPU • ${Math.round(s.memoryBytes / 1024 / 1024)} MiB used`); }, onIdentity: identity => { if (id === this.bootId) this.acceptGuestIdentity(identity); } }); vm.add_listener('serial0-output-byte', v => { if (id === this.bootId && typeof v === 'number') this.serialOutput(v); }); this.scheduleBootWatchdog(id, policy.policy.bootTimeoutMs); this.fit(); vm.run?.(); }
    catch (error) { if (id === this.bootId) this.fail(error instanceof Error ? error.message : String(error), error instanceof Error ? error : new Error(String(error))); }
  }
  public async waitForGuestReady(timeoutMs: number): Promise<void> { const deadline = Date.now() + timeoutMs; if (this.initializationFailure) throw this.initializationFailure; while (!this.emulator && !this.initializationFailure && this.health === 'booting' && Date.now() < deadline) await new Promise(r => setTimeout(r, 50)); if (this.initializationFailure) throw this.initializationFailure; if (!this.emulator) throw new Error('v86 emulator was not created before readiness wait'); const waitVga = this.emulator.wait_until_vga_screen_contains; if (typeof waitVga !== 'function') throw new Error('v86 emulator does not expose wait_until_vga_screen_contains()'); this.bootStage = 'waiting for Alpine VGA'; await waitVga.call(this.emulator, /Alpine Linux|Welcome to Alpine|localhost login:/i, { timeout_msec: Math.max(1, deadline - Date.now()) }); this.vgaReady = true; this.tryMarkReady(); while (!this.ready && !this.initializationFailure && this.health === 'booting' && Date.now() < deadline) await new Promise(r => setTimeout(r, 50)); if (this.initializationFailure) throw this.initializationFailure; if (!this.ready) throw new Error('guest boot did not reach ready state after VGA and Alpine identity verification'); }
  private acceptGuestIdentity(identity: GuestIdentity): void { if (!identity.isAlpine || identity.kind !== this.profile.expectedGuest) { if (identity.kind !== 'unknown') this.fail(`Guest identity mismatch: expected ${this.profile.expectedGuest}`); return; } this.guestIdentity = identity; this.monitor(`${this.profile.memoryMiB} MiB • Alpine identity verified`); this.tryMarkReady(); }
  private tryMarkReady(): void { if (this.ready || !this.vgaReady || !this.guestIdentity?.isAlpine || this.guestIdentity.kind !== this.profile.expectedGuest) return; this.ready = true; this.bootStage = 'VGA + Alpine identity verified'; if (this.bootTimeout !== null) clearTimeout(this.bootTimeout); this.bootTimeout = null; this.setHealth('ready'); this.status(`${this.profile.name} • running`); }
  private detectGuestIdentity(text: string): GuestIdentity | null { if (/(?:^|\r?\n)ID=alpine(?:\r?\n|$)/im.test(text) || /(?:^|\r?\n)NAME="?Alpine Linux/i.test(text) || /Alpine Linux/i.test(text)) return { kind: 'alpine', isAlpine: true, release: text.slice(-4096) }; return null; }
  private serialOutput(byte: number): void { if (!this.enforcer?.acceptSerialByte(1)) { this.fail('VM serial output limit reached'); return; } const ch = String.fromCharCode(byte & 255); this.lastSerialAt = Date.now(); this.serial = (this.serial + ch).slice(-65536); const identity = this.detectGuestIdentity(this.serial); if (identity) this.acceptGuestIdentity(identity); const accepted = this.enforcer.acceptOutput(ch); if (accepted.value) this.term.write(accepted.value); }
  private canUseRuntime(): boolean { return Boolean(this.emulator && this.enforcer && !this.enforcer.isStopped() && !this.enforcer.sessionExpired()); }
  private loadRuntime(): Promise<void> { if (typeof window.V86Starter === 'function' || typeof window.V86 === 'function') return Promise.resolve(); if (V86LinuxTerminal.runtimePromise) return V86LinuxTerminal.runtimePromise; V86LinuxTerminal.runtimePromise = new Promise<void>((resolve, reject) => { const script = document.querySelector<HTMLScriptElement>('script[data-linuxlab-v86]'); if (!script) { reject(new Error('Missing static /libv86.js runtime tag')); return; } if (typeof window.V86Starter === 'function' || typeof window.V86 === 'function') { resolve(); return; } const finish = () => { if (typeof window.V86Starter === 'function' || typeof window.V86 === 'function') resolve(); else reject(new Error('Local libv86.js loaded but no V86/V86Starter constructor was exposed')); }; script.addEventListener('load', finish, { once: true }); script.addEventListener('error', () => reject(new Error('Failed to load /libv86.js')), { once: true }); }).catch(error => { V86LinuxTerminal.runtimePromise = null; throw error; }); return V86LinuxTerminal.runtimePromise; }
  private async preflightRuntimeAssets(signal: AbortSignal): Promise<void> { for (const asset of ['/libv86.js', '/v86.wasm', '/seabios.bin', '/vgabios.bin']) { const response = await fetch(asset, { method: 'HEAD', cache: 'no-store', signal }); if (!response.ok) throw new Error(`Required VM asset failed to load: ${asset} (${response.status})`); } }
  private scheduleBootWatchdog(id: number, timeoutMs: number): void { if (this.bootTimeout !== null) clearTimeout(this.bootTimeout); this.bootTimeout = setTimeout(() => { if (!this.ready && id === this.bootId) this.fail('guest boot timeout'); }, timeoutMs); }
  private fail(message: string, cause?: Error): void { this.initializationFailure = cause ?? new Error(message); this.ready = false; this.bootStage = 'error'; this.setHealth('offline'); if (this.bootTimeout !== null) { clearTimeout(this.bootTimeout); this.bootTimeout = null; } if (this.sessionTimer !== null) { clearTimeout(this.sessionTimer); this.sessionTimer = null; } this.enforcer?.stop(); try { this.emulator?.stop?.(); this.emulator?.destroy?.(); } catch {} this.emulator = null; this.status(`${this.profile.name} • ${message}`); this.monitor(`Offline • ${message}`); this.term.writeln(`\r\n[VM error] ${message}`); }
  private showTerminal(): void { const s = document.getElementById('screen_container'); const t = document.getElementById('v86-terminal-container'); if (s) s.hidden = true; if (t) t.hidden = false; this.fit(); this.term.focus(); }
  private showScreen(): void { const s = document.getElementById('screen_container'); const t = document.getElementById('v86-terminal-container'); if (s) s.hidden = false; if (t) t.hidden = true; this.fit(); }
  private status(text: string): void { const e = document.getElementById('v86-status'); if (e) e.textContent = text; }
  private monitor(text: string): void { const e = document.getElementById('v86-monitor'); if (e) e.textContent = text; }
  private setHealth(state: 'booting' | 'ready' | 'offline'): void { this.health = state; const e = document.getElementById('v86-health'); if (e) { e.dataset.state = state; e.setAttribute('aria-label', state === 'ready' ? 'VM working' : state === 'booting' ? 'VM booting' : 'VM offline'); } }
  private fit(): void { try { this.fitAddon.fit(); } catch {} }
  private showDiagnostics(): void { const e = document.getElementById('v86-diagnostics'); if (!e) return; e.hidden = !e.hidden; if (!e.hidden) e.textContent = `Profile: ${this.profile.name}\nStage: ${this.bootStage}\nHealth: ${this.health}\nVGA: ${this.vgaReady ? 'verified' : 'pending'}\nGuest: ${this.guestIdentity?.kind || 'unknown'}\nInitialization failure: ${this.initializationFailure?.message || 'none'}\nLast serial: ${this.lastSerialAt ? new Date(this.lastSerialAt).toISOString() : 'none'}`; }
  private async copyDiagnostics(): Promise<void> { const e = document.getElementById('v86-diagnostics'); if (!e) return; if (e.hidden) this.showDiagnostics(); try { await navigator.clipboard.writeText(e.textContent || ''); } catch {} }
  private async dispose(): Promise<void> { if (this.bootTimeout !== null) { clearTimeout(this.bootTimeout); this.bootTimeout = null; } if (this.sessionTimer !== null) { clearTimeout(this.sessionTimer); this.sessionTimer = null; } this.telemetryDispose?.(); this.telemetryDispose = null; const vm = this.emulator; this.emulator = null; this.enforcer?.stop(); this.enforcer = null; try { vm?.stop?.(); vm?.destroy?.(); } catch {} }
  public destroy(): void { this.bootId++; this.bootController?.abort(); this.bootController = null; void this.dispose(); this.terminalDataDisposable?.dispose(); this.terminalDataDisposable = null; try { this.term.dispose(); } catch {} }
}
window.addEventListener('DOMContentLoaded', () => { const vmTerminal = new V86LinuxTerminal(); window.linuxLabVM = vmTerminal; });
