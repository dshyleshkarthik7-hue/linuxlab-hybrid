export {};

type IdentityUser={email?:string;user_metadata?:{full_name?:string}};type NetlifyIdentity={on(event:string,callback:(user?:IdentityUser)=>void):void;currentUser():IdentityUser|null;open(mode?:string):void};type IdentityWindow=Window&{netlifyIdentity?:NetlifyIdentity};
window.addEventListener('DOMContentLoaded',()=>{const identity=(window as IdentityWindow).netlifyIdentity,button=document.querySelector<HTMLButtonElement>('#login'),status=document.querySelector<HTMLElement>('#status');if(!button||!status)return;if(!identity){status.textContent='Netlify Identity could not load. Please try again.';return;}const params=new URLSearchParams(location.search),candidate=params.get('returnTo')||'/beginner/#tutor',returnTo=candidate.startsWith('/')&&!candidate.startsWith('//')?candidate:'/beginner/#tutor';const update=(user?:IdentityUser|null)=>{if(user){const name=user.user_metadata?.full_name||user.email||'your account';status.textContent=`Signed in as ${name}.`;button.textContent='Open account';button.setAttribute('aria-label','Open your Netlify Identity account');}else{status.textContent='Sign in to unlock the AI Tutor.';button.textContent='Sign in / Create account';button.setAttribute('aria-label','Sign in or create a LinuxTerminal account');}};identity.on('init',(user?:IdentityUser)=>update(user));identity.on('login',(user?:IdentityUser)=>{status.textContent=`Signed in as ${user?.email||'your account'}. Returning…`;window.setTimeout(()=>{window.location.href=returnTo;},300);});identity.on('logout',()=>update(null));identity.on('error',()=>{status.textContent='Netlify Identity could not complete that action. Please try again.';});button.addEventListener('click',()=>{identity.open(identity.currentUser()?undefined:'login');});});