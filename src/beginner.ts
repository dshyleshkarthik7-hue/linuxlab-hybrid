import { InBrowserLinuxEngine } from './engine/LinuxEngine';import { COMMAND_LESSONS } from './commands/commandCatalog';
const engine=new InBrowserLinuxEngine();const output=document.querySelector<HTMLDivElement>('#output')!;const input=document.querySelector<HTMLInputElement>('#input')!;const list=document.querySelector<HTMLDivElement>('#commandList')!;const search=document.querySelector<HTMLInputElement>('#search')!;const categories=document.querySelector<HTMLDivElement>('#categories')!;const tutorBody=document.querySelector<HTMLDivElement>('#tutorBody')!;const tutorInput=document.querySelector<HTMLInputElement>('#tutorInput')!;let active='All';let history:string[]=[];let tutorRequests=0;let tutorWindow=Date.now();
const write=(text:string,cls='')=>{const d=document.createElement('div');d.className=`line ${cls}`;d.textContent=text;output.appendChild(d);output.scrollTop=output.scrollHeight};const prompt=()=>{const p=document.querySelector('.input-row span');if(p)p.textContent=`root@linuxterminal:${engine.getCwd()==='/root'?'~':engine.getCwd()}#`};
async function run(value:string){const command=value.trim();if(!command)return;write(`root@linuxterminal:~# ${command}`,'cmd');input.value='';const result=await engine.execute(command);if(result)write(result);history.push(command);if(history.length>20)history.shift();prompt()}
const cats=['All',...Array.from(new Set(COMMAND_LESSONS.map(c=>c.category)))];for(const c of cats){const b=document.createElement('button');b.textContent=c;if(c==='All')b.classList.add('active');b.onclick=()=>{active=c;document.querySelectorAll('.chips button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render()};categories.appendChild(b)}
function render(){const q=search.value.trim().toLowerCase();list.replaceChildren();COMMAND_LESSONS.filter(c=>(active==='All'||c.category===active)&&(!q||c.name.includes(q)||c.summary.toLowerCase().includes(q))).forEach(lesson=>{const row=document.createElement('div');row.className='command';const info=document.createElement('div');const code=document.createElement('code');code.textContent=lesson.name;const desc=document.createElement('small');desc.textContent=lesson.summary;info.append(code,desc);const btn=document.createElement('button');btn.className='try';btn.textContent='TRY';btn.onclick=()=>{input.value=lesson.example;input.focus()};row.append(info,btn);row.onclick=e=>{if(e.target!==btn){input.value=lesson.example;input.focus()}};list.appendChild(row)});document.querySelector('#count')!.textContent=String(COMMAND_LESSONS.length)}
async function askTutor(question:string){const q=question.trim();if(!q)return;const now=Date.now();if(now-tutorWindow>=60000){tutorWindow=now;tutorRequests=0}if(tutorRequests>=6){tutorBody.textContent='Tutor limit reached for this minute. Keep practicing and try again shortly.';return}tutorRequests++;tutorBody.textContent='Thinking…';const context=`Level: beginner\nCurrent directory: ${engine.getCwd()}\nRecent commands: ${history.slice(-8).join(' | ')||'none'}\nLast command: ${history.at(-1)||'none'}\nEnvironment: browser-safe Linux simulator`;try{const r=await fetch('/api/tutor',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question:q.slice(0,1200),context})});const data=await r.json() as {answer?:string;error?:string};if(!r.ok)throw new Error(data.error||'Tutor unavailable');tutorBody.textContent=data.answer||'No answer returned.'}catch(e){tutorBody.textContent=e instanceof Error?e.message:'Tutor unavailable.'}}
search.addEventListener('input',render);document.querySelector<HTMLFormElement>('#commandForm')!.addEventListener('submit',e=>{e.preventDefault();void run(input.value)});document.querySelector('#reset')!.addEventListener('click',()=>location.reload());document.querySelectorAll<HTMLButtonElement>('.suggestions button').forEach(b=>b.onclick=()=>void askTutor(b.dataset.q||''));document.querySelector<HTMLFormElement>('#tutorForm')!.addEventListener('submit',e=>{e.preventDefault();void askTutor(tutorInput.value);tutorInput.value=''});document.querySelector('#tutorToggle')?.addEventListener('click',()=>document.querySelector('#tutor')?.scrollIntoView({behavior:'smooth'}));
const keyboard=document.querySelector<HTMLDivElement>('#keyboard')!;for(const [label,value] of [['ESC','\\u001b'],['TAB','\\t'],['CTRL+C','\\u0003'],['CTRL+D','\\u0004'],['|','|'],['/','/'],['~','~'],['ENTER','\\n']] as const){const b=document.createElement('button');b.type='button';b.textContent=label;b.onclick=()=>{if(value==='\\n')void run(input.value);else input.setRangeText(value,input.selectionStart??input.value.length,input.selectionEnd??input.value.length,'end');input.focus()};keyboard.appendChild(b)}
write('Welcome to LinuxTerminal.me Beginner Sandbox. All 200 catalog lessons are searchable.');write('Engine A is a browser simulation, not a real Linux kernel. Use Real Linux for genuine kernel behavior.');write('Try `pwd`, `ls -la`, `mkdir practice && touch practice/hello.txt`, or `cat /etc/os-release`.');render();