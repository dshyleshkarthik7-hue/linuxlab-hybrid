document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-tutorial]').forEach(button=>{const id=button.getAttribute('data-tutorial')||'';const title=button.getAttribute('data-title')||id;const state=window.LinuxProgress?.read();const done=state?.tutorials?.some(x=>x.id===id);if(done){button.classList.add('done');button.textContent='✓ Completed';}button.addEventListener('click',()=>{window.LinuxProgress?.completeTutorial(id,title);button.classList.add('done');button.textContent='✓ Completed';});});});