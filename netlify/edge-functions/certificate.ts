const EXAM_VERSION='linux-foundations-1.0';const QUESTION_COUNT=30;const PASS_PERCENT=80;const ATTEMPT_TTL=3600;const CERT_TTL=60*60*24*365*5;const UPSTASH_URL=Netlify.env.get('UPSTASH_REDIS_REST_URL');const UPSTASH_TOKEN=Netlify.env.get('UPSTASH_REDIS_REST_TOKEN');const SIGNING_SECRET=Netlify.env.get('CERTIFICATE_SIGNING_SECRET');const ORIGIN=Netlify.env.get('TUTOR_ALLOWED_ORIGINS')||'https://linuxterminal.me';
type User={id?:unknown;email?:unknown;user_metadata?:{full_name?:unknown}};type Q={id:string;q:string;c:string[];a:string;t:string};type Attempt={userId:string;questions:Q[];version:string;startedAt:string};type Cert={id:string;userId:string;name:string;score:number;total:number;percentage:number;passed:true;issuedAt:string;version:string;signature:string};
const BANK:Q[]=[{id:'pwd',q:'Which command prints the current working directory?',c:['pwd','cd','ls','whoami'],a:'pwd',t:'navigation'},{id:'ls',q:'Which ls form commonly includes hidden entries and long metadata?',c:['ls -la','ls -r','ls -p','ls -n'],a:'ls -la',t:'files'},{id:'cd',q:'What does cd .. normally do?',c:['moves to the parent directory','prints the parent directory','removes the parent','copies the parent'],a:'moves to the parent directory',t:'navigation'},{id:'mkdir',q:'What does mkdir -p add?',c:['creates missing parent directories','prints permissions','removes parents','compresses directories'],a:'creates missing parent directories',t:'files'},{id:'cat',q:'What does cat notes.txt normally write to stdout?',c:['the file contents','only permissions','the username','only the size'],a:'the file contents',t:'files'},{id:'cp',q:'Which command copies report.txt to backup.txt?',c:['cp report.txt backup.txt','mv report.txt backup.txt','ln report.txt backup.txt','cat report.txt backup.txt'],a:'cp report.txt backup.txt',t:'files'},{id:'mv',q:'What is a common use of mv?',c:['move or rename a path','show processes','search text','change permissions'],a:'move or rename a path',t:'files'},{id:'rm',q:'Which flag is commonly needed to remove a directory tree?',c:['-r','-l','-n','-p'],a:'-r',t:'files'},{id:'find',q:'Which find test restricts matches to regular files?',c:['-type f','-kind file','-regular','-file'],a:'-type f',t:'search'},{id:'grep',q:'What does grep -n add to matching lines?',c:['line numbers','permissions','timestamps','network addresses'],a:'line numbers',t:'text'},{id:'head',q:'What does head -n 5 request?',c:['the first five lines','the last five lines','five matching lines','five bytes'],a:'the first five lines',t:'text'},{id:'tail',q:'What does tail -n 20 request?',c:['the last twenty lines','the first twenty lines','twenty words','twenty files'],a:'the last twenty lines',t:'text'},{id:'wc',q:'Which wc option counts lines?',c:['-l','-c','-w','-m'],a:'-l',t:'text'},{id:'sort',q:'What does sort -n change?',c:['numeric comparison','duplicate removal','reverse filesystem','word counting'],a:'numeric comparison',t:'text'},{id:'uniq',q:'Why is sort often before uniq for arbitrary text?',c:['uniq compares adjacent duplicates','sort encrypts input','uniq accepts only numbers','the shell requires sort'],a:'uniq compares adjacent duplicates',t:'text'},{id:'sed',q:'What does sed s/old/new/g represent?',c:['a substitution on input lines','a permission change','a process search','a network request'],a:'a substitution on input lines',t:'text'},{id:'awk',q:'What does $1 normally mean in awk?',c:['the first field of the current record','the shell PID','the first shell argument','the first output file'],a:'the first field of the current record',t:'text'},{id:'chmod',q:'What does chmod change?',c:['file permission bits','file contents','the current directory','the kernel version'],a:'file permission bits',t:'permissions'},{id:'whoami',q:'What does whoami report?',c:['the effective username','all users','the hostname only','the current directory'],a:'the effective username',t:'users'},{id:'ps',q:'What does ps normally provide?',c:['a process snapshot','an archive','an editor','a packet capture'],a:'a process snapshot',t:'processes'},{id:'df',q:'Which tool reports filesystem capacity and available space?',c:['df','du','free','wc'],a:'df',t:'storage'},{id:'du',q:'Which tool estimates directory-tree space usage?',c:['du','df','free','ps'],a:'du',t:'storage'},{id:'free',q:'What does free report?',c:['memory and swap statistics','filesystem capacity','process IDs','network routes'],a:'memory and swap statistics',t:'system'},{id:'uname',q:'Which command shows kernel/system identification?',c:['uname','whoami','date','env'],a:'uname',t:'system'},{id:'tar',q:'Which tar operation creates an archive?',c:['-c','-x','-t','-p'],a:'-c',t:'archives'},{id:'gzip',q:'What is gzip primarily for?',c:['compression','encryption','filesystem repair','process scheduling'],a:'compression',t:'archives'},{id:'curl',q:'What does curl -I commonly request?',c:['HTTP response headers','a local file listing','an SSH key','a mount'],a:'HTTP response headers',t:'network'},{id:'ip',q:'Which command commonly inspects Linux interface addresses?',c:['ip addr','ifconfig only','routefile','netstat-file'],a:'ip addr',t:'network'},{id:'gcc',q:'What does gcc -o app main.c do when compilation succeeds?',c:['builds a C executable named app','opens the source','compresses it','runs ping'],a:'builds a C executable named app',t:'development'},{id:'make',q:'What does make primarily use to decide what to build?',c:['Makefile rules and dependencies','the username','the browser URL','terminal color'],a:'Makefile rules and dependencies',t:'development'},{id:'pipe',q:'What does grep ERROR app.log | wc -l demonstrate?',c:['sending grep stdout into wc','copying the file','running commands independently','redirecting stderr'],a:'sending grep stdout into wc',t:'shell'},{id:'redirect',q:'What does > normally do?',c:['redirect stdout and replace the destination','append stdout','redirect stderr only','create a pipeline'],a:'redirect stdout and replace the destination',t:'shell'},{id:'append',q:'What does >> normally do?',c:['append stdout to a file','replace stdout with stderr','create a pipeline','change permissions'],a:'append stdout to a file',t:'shell'},{id:'status',q:'What does a non-zero exit status normally indicate?',c:['failure or a false condition','successful output','root execution','directory change'],a:'failure or a false condition',t:'shell'},{id:'export',q:'What is export mainly used for?',c:['make a variable available to child processes','compress a variable','delete a variable','show disk usage'],a:'make a variable available to child processes',t:'shell'},{id:'ssh',q:'What does SSH provide for a remote shell?',c:['encrypted transport and authentication','compression only','a local editor','a compiler'],a:'encrypted transport and authentication',t:'network'},{id:'ping',q:'What does ping test?',c:['ICMP echo reachability and timing','all TCP ports','disk capacity','file permissions'],a:'ICMP echo reachability and timing',t:'network'},{id:'cut',q:'Which cut options commonly select delimiter-separated fields?',c:['-d and -f','-n and -r','-x and -z','-p and -q'],a:'-d and -f',t:'text'}];
function shuffle<T>(x:readonly T[]){const a=[...x];for(let i=a.length-1;i>0;i--){const r=new Uint32Array(1);crypto.getRandomValues(r);const j=r[0]%(i+1);[a[i],a[j]]=[a[j],a[i]]}return a}
const json=(x:Record<string,unknown>,status=200)=>new Response(JSON.stringify(x),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','access-control-allow-origin':ORIGIN,'access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'content-type,authorization'}});
async function auth(r:Request):Promise<User|null>{const a=r.headers.get('authorization'),c=r.headers.get('cookie');if(!a&&!c)return null;const h=new Headers();if(a?.match(/^Bearer\s+\S+$/i))h.set('authorization',a);else if(c)h.set('cookie',c);else return null;try{const z=await fetch(new URL('/.netlify/identity/user',r.url),{headers:h,cache:'no-store'});if(!z.ok)return null;const u=await z.json() as User;return typeof u.id==='string'&&u.id.trim()?u:null}catch{return null}}
async function redis(command:unknown[]){if(!UPSTASH_URL||!UPSTASH_TOKEN)throw new Error('Upstash is not configured');const r=await fetch(UPSTASH_URL,{method:'POST',headers:{authorization:`Bearer ${UPSTASH_TOKEN}`,'content-type':'application/json'},body:JSON.stringify(command)});if(!r.ok)throw new Error('Upstash request failed');const x=await r.json() as {result?:unknown,error?:unknown};if(x.error)throw new Error(String(x.error));return x.result}
const safe=(x:string)=>x.replace(/[^a-zA-Z0-9:._-]/g,'_');async function sign(v:string){if(!SIGNING_SECRET)throw new Error('Certificate signing is not configured');const k=await crypto.subtle.importKey('raw',new TextEncoder().encode(SIGNING_SECRET),{name:'HMAC',hash:'SHA-256'},false,['sign']);const s=await crypto.subtle.sign('HMAC',k,new TextEncoder().encode(v));return [...new Uint8Array(s)].map(b=>b.toString(16).padStart(2,'0')).join('')}
const canonical=(x:Omit<Cert,'signature'>)=>[x.id,x.userId,x.name,x.score,x.total,x.percentage,x.issuedAt,x.version].join('|');const publicCert=(x:Cert)=>({id:x.id,name:x.name,score:x.score,total:x.total,percentage:x.percentage,passed:true,issuedAt:x.issuedAt,version:x.version});
async function start(user:User){if(!UPSTASH_URL||!UPSTASH_TOKEN||!SIGNING_SECRET)return json({error:'The free verified exam is temporarily unavailable because persistence or signing is not configured.'},503);const selected=shuffle(BANK).slice(0,QUESTION_COUNT).map(q=>({...q,c:shuffle(q.c)}));const id=crypto.randomUUID();const attempt:Attempt={userId:String(user.id),questions:selected,version:EXAM_VERSION,startedAt:new Date().toISOString()};await redis(['SET',`linuxterminal:exam:attempt:${id}`,JSON.stringify(attempt),'EX',ATTEMPT_TTL]);return json({attemptId:id,version:EXAM_VERSION,total:QUESTION_COUNT,passPercent:PASS_PERCENT,questions:selected.map(({a,...q})=>q)})}
async function submit(user:User,b:unknown){if(!UPSTASH_URL||!UPSTASH_TOKEN||!SIGNING_SECRET)return json({error:'Verified exam is not configured.'},503);if(!b||typeof b!=='object')return json({error:'Invalid request body.'},400);const v=b as {attemptId?:unknown;answers?:unknown};if(typeof v.attemptId!=='string'||!Array.isArray(v.answers)||v.answers.length!==QUESTION_COUNT)return json({error:`Exactly ${QUESTION_COUNT} answers are required.`},400);const k=`linuxterminal:exam:attempt:${safe(v.attemptId)}`,lock=`${k}:lock`;if(await redis(['SET',lock,String(user.id),'EX',60,'NX'])!=='OK')return json({error:'This attempt is already being submitted.'},409);try{const raw=await redis(['GET',k]);if(typeof raw!=='string')return json({error:'Exam attempt expired or was not found.'},404);const attempt=JSON.parse(raw) as Attempt;if(attempt.userId!==String(user.id)||attempt.version!==EXAM_VERSION)return json({error:'Exam attempt does not belong to this account.'},403);const answers=new Map<string,number>();for(const item of v.answers){if(!item||typeof item!=='object')return json({error:'Invalid answer entry.'},400);const a=item as {id?:unknown;choice?:unknown};if(typeof a.id!=='string'||!Number.isInteger(a.choice)||answers.has(a.id))return json({error:'Each question must be answered exactly once.'},400);answers.set(a.id,a.choice as number)}if(answers.size!==QUESTION_COUNT)return json({error:'Every exam question must be answered exactly once.'},400);let score=0;for(const q of attempt.questions)if(answers.get(q.id)===q.c.indexOf(q.a))score++;const percentage=Math.round(score/QUESTION_COUNT*10000)/100;await redis(['DEL',k]);if(percentage<PASS_PERCENT)return json({passed:false,score,total:QUESTION_COUNT,percentage,version:EXAM_VERSION,message:'Not passed. Review the tutorials and retake the free assessment.'});const issuedAt=new Date().toISOString();const name=typeof user.user_metadata?.full_name==='string'?user.user_metadata.full_name.trim().slice(0,120):(typeof user.email==='string'?user.email.slice(0,120):'LinuxTerminal learner');const id=`LT-LNX-${new Date().getUTCFullYear()}-${crypto.randomUUID().replaceAll('-','').slice(0,10).toUpperCase()}`;const unsigned:Omit<Cert,'signature'>={id,userId:String(user.id),name:name||'LinuxTerminal learner',score,total:QUESTION_COUNT,percentage,passed:true,issuedAt,version:EXAM_VERSION};const certificate:Cert={...unsigned,signature:await sign(canonical(unsigned))};await redis(['SET',`linuxterminal:certificate:${id}`,JSON.stringify(certificate),'EX',CERT_TTL]);return json({certificate:publicCert(certificate),verificationPath:`/verify/?id=${encodeURIComponent(id)}`})}finally{await redis(['DEL',lock]).catch(()=>{})}}
async function verify(id:string){if(!id||id.length>80)return json({error:'A certificate ID is required.'},400);const raw=await redis(['GET',`linuxterminal:certificate:${safe(id)}`]);if(typeof raw!=='string')return json({error:'Certificate not found.'},404);const c=JSON.parse(raw) as Cert;const {signature,...unsigned}=c;if(await sign(canonical(unsigned))!==signature)return json({error:'Certificate signature verification failed.'},500);return json({certificate:publicCert(c)})}
export default async(r:Request)=>{if(r.method==='OPTIONS')return new Response('',{status:204,headers:{'access-control-allow-origin':ORIGIN,'access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'content-type,authorization'}});try{const u=new URL(r.url);if(r.method==='GET')return verify(u.searchParams.get('id')||'');if(r.method!=='POST')return json({error:'method not allowed'},405);const user=await auth(r);if(!user)return json({error:'Sign in to take the free verified exam.'},401);const body=await r.json().catch(()=>({}));return u.searchParams.get('action')==='start'?start(user):u.searchParams.get('action')==='submit'?submit(user,body):json({error:'Unknown action.'},400)}catch(e){console.error('Certificate service failed',e instanceof Error?e.message:String(e));return json({error:'Certificate service unavailable.'},503)}};
export const config={path:'/api/certificate'};
